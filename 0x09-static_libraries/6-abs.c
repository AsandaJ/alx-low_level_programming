#include "main.h"


/**
 * _abs - a function that calculatesabsolute value
 * @n: value to be checked
 * Return: absolute integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
