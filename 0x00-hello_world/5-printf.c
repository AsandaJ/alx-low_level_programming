#include <stdio.h>
/**
 * main - prints exactly texts mentioned and new line
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
puts("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
